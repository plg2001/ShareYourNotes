<div class="container">
  <%= form_with(model: richiesta_admin) do |form| %>
    <% if richiesta_admin.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(richiesta_admin.errors.count, "error") %> prohibited this richiesta_admin from being saved:</h2>
        <ul>
          <% richiesta_admin.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div class="field">
      <%= form.label :user_id %>
      <%= form.text_field :user_id, value: current_user.id %>
    </div>
    <div class="field">
      <%= form.label :content %>
      <%= form.text_area :content, value: "Salve desidero diventare un Amministratore del vostro sito"%>
    </div>
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</div>
